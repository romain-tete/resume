import { HideOnPrintDirective } from './hide-on-print.directive';

describe('HideOnPrintDirective', () => {
  it('should create an instance', () => {
    const directive = new HideOnPrintDirective();
    expect(directive).toBeTruthy();
  });
});
