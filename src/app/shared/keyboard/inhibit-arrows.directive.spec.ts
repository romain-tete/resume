import { InhibitArrowsDirective } from './inhibit-arrows.directive';

describe('InhibitArrowsDirective', () => {
  it('should create an instance', () => {
    const directive = new InhibitArrowsDirective();
    expect(directive).toBeTruthy();
  });
});
