import { ResourceFormFactoryDirective } from './resource-form-factory.directive';

describe('ResourceFormFactoryDirective', () => {
  it('should create an instance', () => {
    const directive = new ResourceFormFactoryDirective();
    expect(directive).toBeTruthy();
  });
});
